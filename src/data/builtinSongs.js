// Built-in song library — currently empty.
// (Premade songs were removed at the user's request — they didn't sound good.
// Users can still import their own .mid/.midi files via the Piano's import button.)

export const BUILTIN_SONGS = []
